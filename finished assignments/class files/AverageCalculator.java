import java.util.Arrays;
import java.util.Scanner;

public class AverageCalculator {

    /*
    public static void main(String[] args) {

        float input = 0, last = 0, ave = 0, amount = 0, sum = 0, entered = 0;
        Scanner in = new Scanner(System.in);

        System.out.println("How many numbers do you want to calculate the average for:");
        amount = in.nextInt();


        System.out.println("Enter the numbers 'one by one':");

        for (int i = 0; i < amount; i++) {
            if (in.hasNextLine()) {
                input = in.nextInt();
                if (input != -1) {
                    sum += input;
                    entered++;
                } else {
                    amount++;
                    ave = sum / entered;
                    System.out.println("The current average of the entered numbers is: " + ave);
                }
            }
        }

        ave = sum / entered;

        System.out.println("The average of the entered numbers is: " + ave);

    }
    */

}